import React from "react";

export default function About() {
  return (
    <section className="about glass">
      <h2>About WhiteShadow Pair</h2>
      <p className="muted">This interface lets trusted users request pairing codes for WhatsApp bot instances securely. Backed by MongoDB and JWT auth on the server.</p>
    </section>
  );
}
